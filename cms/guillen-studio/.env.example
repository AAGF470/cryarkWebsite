# Payload pilot env — copy to .env (never commit the filled-in version).
PAYLOAD_SECRET=generate_a_long_random_string
DB_PASSWORD=generate_a_strong_db_password
PAYLOAD_PUBLIC_SERVER_URL=https://cms.guillen.studio
DATABASE_URI=postgres://payload:${DB_PASSWORD}@guillen-cms-db:5432/payload
